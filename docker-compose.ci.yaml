services:
  postgres:
    image: postgres:latest
    environment:
      POSTGRES_USER: draftpythonapi
      POSTGRES_PASSWORD: draftpythonapi
      POSTGRES_DB: draftpythonapi
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
    name: pgdata
